spring:
  mongodb:
    uri: "${MONGODB_URI:mongodb+srv://juandiego:perroloco90@geolocalizacion.j7gyrmc.mongodb.net/geolocalization}"
  rabbitmq:
    host: ${RABBITMQ_HOST:woodpecker.rmq.cloudamqp.com}
    port: ${RABBITMQ_PORT:5671}
    username: ${RABBITMQ_USERNAME:thjdybjd}
    password: ${RABBITMQ_PASSWORD:eHWjGdLhNYN73Ct96wUAV1KkpwnDiKZj}
    virtual-host: ${RABBITMQ_VIRTUAL_HOST:thjdybjd}
    ssl:
      enabled: ${RABBITMQ_SSL:true}

server:
  port: ${SERVER_PORT:8080}

auth:
  service:
    url: ${AUTH_SERVICE_URL:http://localhost:8080}

gamification:
  service:
    url: ${GAMIFICATION_SERVICE_URL:http://localhost:8080}

user:
  service:
    url: ${USER_SERVICE_URL:http://localhost:8099}

parche:
  service:
    url: ${PARCHE_SERVICE_URL:http://localhost:8085}

campus:
  events:
    service:
      url: ${CAMPUS_EVENTS_SERVICE_URL:http://localhost:8086}

internal:
  service:
    token: ${INTERNAL_SERVICE_TOKEN:snorlax-internal-secret-geo-2025}

geo:
  campus:
    default-radius-meters: ${GEO_DEFAULT_RADIUS:500}

google:
  maps:
    api-key: ${GOOGLE_MAPS_API_KEY:}

springdoc:
  swagger-ui:
    use-root-path: true
  servers:
    - url: ${SERVER_URL:http://localhost:8080}
